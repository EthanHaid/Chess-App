public class Simulation extends Player {
   public Simulation(Board board, int startPos, int team) { //sim constructor
      super(board, startPos, team);
   }
   public void makeMove() throws InterruptedException {
      return;
   }
}
